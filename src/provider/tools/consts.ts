/**
 * 工具常量 — Copilot Custom Bridge (Phase 2)
 */

/** activate_ 预热工具前缀 */
export const PREFLIGHT_TOOL_PREFIX = 'activate_';

/** 默认工具数量上限 */
export const DEFAULT_MAX_TOOLS = 128;

/** 工具调用最大循环轮次 (硬编码) */
export const MAX_TOOL_CALL_ROUNDS = 10;
